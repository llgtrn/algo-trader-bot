# Runtime deps for the no-Docker path: the cTrader MCP server + the optional
# 24/7 daemon. (The brain reuses agents/tradingagents/requirements.txt.)
#
#   python -m venv .venv && . .venv/bin/activate   # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
mcp>=0.9.0                 # Model Context Protocol server (cTrader MCP)
ctrader-open-api==0.9.2    # cTrader Open API client
twisted>=23.0.0            # async reactor used by the cTrader client
python-dotenv>=1.0.0       # .env loading
requests>=2.31.0           # Telegram + token refresh HTTP
pandas>=2.0.0              # OHLCV frames + indicators
numpy>=1.24.0             # indicator math
